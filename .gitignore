# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes

# Xcode user-specific state
xcuserdata/
*.xcuserstate
*.xccheckout
*.moved-aside
DerivedData/
build/
Build/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# XcodeGen source (xcodeproj is committed directly; project.yml is no longer needed)
project.yml

# Swift Package Manager
.build/
Packages/
Package.resolved
.swiftpm/

# CocoaPods (if added later)
Pods/

# Carthage (if added later)
Carthage/Build/

# fastlane (if added later)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Editors
.vscode/
.idea/
*.swp
*.swo

# Local config / secrets
*.env
.env.local
.env.*.local
ExportOptions.plist

# Runtime / user data (imported wallpapers live in user Application Support)
/var/
/tmp/

# Development-only docs (not shipped)
OPTIMIZATION.md
mac-lively-wrapper-expert-prompt.md
CLAUDE.md
AGENTS.md
release


#Ignore insiders AI rules
.github/instructions/codacy.instructions.md
